# 07 — Sprint 6: API Client Layer

## Status: COMPLETED

## Objective

Build the client-side API layer: TypeScript types for the API response envelope, a base fetch wrapper that attaches JWT tokens, handles 401 responses (attempts token refresh, redirects to login on failure), and per-domain API modules for each backend domain.

## Prerequisites

- **Plan 04 must be complete** (root layout, env vars)
- **Plan 05 must be complete** (auth-store exists with `useAuthStore` exporting `accessToken`, `refreshToken`, `expiresAt`, `login`, `logout`, `setAccessToken`)
- Read these files before starting:
  - `docs/outlines/05_NEXTJS_FRONTEND.md` — BFF pattern, data flow (Section 4)
  - `docs/outlines/06_API_GATEWAY_AUTH.md` — Gateway routes, JWT format, token refresh endpoint
  - `frontend/src/lib/store/auth-store.ts` — auth store API (you will import `useAuthStore`)
  - `frontend/AGENTS.md` — Next.js 16 docs reference
  - `frontend/.env.local` — `NEXT_PUBLIC_GATEWAY_URL` value

## Architecture Notes

- **The API client runs in the browser** (Client Components call these functions). Server Components call BFF route handlers directly (Plan 08), not this client.
- **Token refresh flow:** When a 401 is received, attempt `POST /api/auth/refresh` with the refresh token. If that succeeds, retry the original request. If refresh fails, clear auth state and redirect to `/login`.
- **ApiResponse<T> envelope:** The API Gateway wraps all responses in `{ success: boolean, data?: T, message?: string, errors?: Record<string, string[]> }`. The client unwraps this — callers receive `T` directly on success and a thrown `ApiError` on failure.
- **No axios.** Use the native `fetch` API. All dependencies are already in `package.json` — no new packages needed.

## Steps

### Step 1: Create TypeScript types for the API layer

- [x] Create `frontend/src/lib/api/types.ts` with `ApiResponse<T>`, `ApiError`, and `PageResponse<T>`

### Step 2: Create the base API client

- [x] Create `frontend/src/lib/api/client.ts` with `apiClient<T>()` function
- [x] `apiClient` prepends `GATEWAY_URL` to all requests
- [x] `apiClient` attaches `Authorization: Bearer <token>` header from auth-store
- [x] `apiClient` handles 401 by attempting `/api/auth/refresh` and retrying once
- [x] `apiClient` deduplicates concurrent refresh attempts (single in-flight promise)
- [x] `apiClient` redirects to `/login` on refresh failure
- [x] `apiClient` parses `ApiResponse<T>` envelope and throws `ApiError` on failure

### Step 3: Create per-domain API modules

- [x] Create `frontend/src/lib/api/auth.ts` with `login()`, `register()`, `validateToken()`
- [x] Create `frontend/src/lib/api/customers.ts` with CRUD functions
- [x] Create `frontend/src/lib/api/insurances.ts` with list/get functions
- [x] Create `frontend/src/lib/api/estimations.ts` with list/get/create functions
- [x] Create `frontend/src/lib/api/vehicles.ts` with list/get functions

### Step 4: Create an API barrel export

- [x] Create `frontend/src/lib/api/index.ts` with barrel re-exports

### Step 5: Verify build

- [x] `cd frontend && npm run build` passes

### Step 6: Verify lint

- [x] `cd frontend && npm run lint` passes

## Acceptance Criteria

- [x] `frontend/src/lib/api/types.ts` exists with `ApiResponse<T>`, `ApiError`, and `PageResponse<T>`
- [x] `frontend/src/lib/api/client.ts` exists with `apiClient<T>()` function
- [x] `apiClient` prepends `GATEWAY_URL` to all requests
- [x] `apiClient` attaches `Authorization: Bearer <token>` header from auth-store
- [x] `apiClient` handles 401 by attempting `/api/auth/refresh` and retrying once
- [x] `apiClient` deduplicates concurrent refresh attempts (single in-flight promise)
- [x] `apiClient` redirects to `/login` on refresh failure
- [x] `apiClient` parses `ApiResponse<T>` envelope and throws `ApiError` on failure
- [x] `frontend/src/lib/api/auth.ts` exists with `login()`, `register()`, `validateToken()`
- [x] `frontend/src/lib/api/customers.ts` exists with CRUD functions
- [x] `frontend/src/lib/api/insurances.ts` exists with list/get functions
- [x] `frontend/src/lib/api/estimations.ts` exists with list/get/create functions
- [x] `frontend/src/lib/api/vehicles.ts` exists with list/get functions
- [x] `frontend/src/lib/api/index.ts` exists with barrel re-exports
- [x] `npm run build` passes
- [x] `npm run lint` passes
