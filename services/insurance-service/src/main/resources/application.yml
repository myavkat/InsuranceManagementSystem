server:
  port: 8084

spring:
  application:
    name: insurance-service
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5436/insurance_db
    username: ims_user
    password: ims_password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  cloud:
    stream:
      default-binder: kafka
      bindings:
        processInsuranceSaga-in-0:
          destination: estimation.saga
          group: insurance-service-group
      kafka:
        binder:
          brokers: localhost:9092
      dynamicDestinations: estimation.saga
  kafka:
    consumer:
      group-id: insurance-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "com.insurancemanagementsystem.*"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

insurance:
  outbox:
    poll-interval-ms: 1000
    max-retries: 3
    failed-ttl-minutes: 60

outbox:
  poll-interval-ms: ${insurance.outbox.poll-interval-ms:1000}
  max-retries: ${insurance.outbox.max-retries:3}
  failed-ttl-minutes: ${insurance.outbox.failed-ttl-minutes:60}

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} [%X{traceId:-},%X{sagaId:-}] - %msg%n"
  level:
    com.insurancemanagementsystem: DEBUG
